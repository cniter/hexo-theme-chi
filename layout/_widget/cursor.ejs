<% if (theme.cursor.on) { %>
    <style>
        body {
            cursor: url("<%- url_for(theme.cursor.normal) %>"), auto;
        }

        #navbar-toggler--btn, a, button {
            cursor: url("<%- url_for(theme.cursor.link) %>"), auto;
        }
    </style>
<% } %>